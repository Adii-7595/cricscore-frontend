import { Container, Typography } from "@mui/material";

const Login = () => {
    return (
        <Container sx={{ py: 5 }}>
            <Typography variant="h3">
                Login
            </Typography>
        </Container>
    );
};

export default Login;