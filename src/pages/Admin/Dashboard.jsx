import { Container, Typography } from "@mui/material";

const Dashboard = () => {
    return (
        <Container sx={{ py: 5 }}>
            <Typography variant="h3">
                Dashboard
            </Typography>
        </Container>
    );
};

export default Dashboard;