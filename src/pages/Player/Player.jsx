import { Container, Typography } from "@mui/material";

const Player = () => {
    return (
        <Container sx={{ py: 5 }}>
            <Typography variant="h3">
                Player
            </Typography>
        </Container>
    );
};

export default Player;