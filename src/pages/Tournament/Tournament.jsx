import { Container, Typography } from "@mui/material";

const Tournament = () => {
    return (
        <Container sx={{ py: 5 }}>
            <Typography variant="h3">
                Tournament
            </Typography>
        </Container>
    );
};

export default Tournament;